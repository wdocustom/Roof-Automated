# Nixpacks configuration for Railway
# Python app with code in backend/ subdirectory

[phases.setup]
aptPkgs = ["build-essential"]
nixPkgs = ["python311"]

[start]
cmd = "cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
